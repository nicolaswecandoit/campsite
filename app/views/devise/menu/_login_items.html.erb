<% if proprietaire_signed_in? %>
  <li>
  <%= link_to('Logout', destroy_proprietaire_session_path, :method => :delete) %>
  </li>
<% else %>
  <li>
  <%= link_to('Login', new_proprietaire_session_path)  %>
  </li>
<% end %>
