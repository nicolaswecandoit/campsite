<%= form_for(departement) do |f| %>
  <% if departement.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(departement.errors.count, "error") %> prohibited this departement from being saved:</h2>

      <ul>
      <% departement.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :description %>
    <%= f.text_area :description %>
  </div>

  <div class="field">
    <%= f.label :nomdep %>
    <%= f.text_field :nomdep %>
  </div>

  <div class="field">
    <%= f.label :departement_id %>
    <%= f.number_field :departement_id %>
  </div>

  <div class="field">
    <%= f.label :slug %>
    <%= f.text_field :slug %>
  </div>

  <div class="field">
    <%= f.label :region_id %>
    <%= f.number_field :region_id %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
